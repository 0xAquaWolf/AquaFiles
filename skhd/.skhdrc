# === Ignore apps ===
.blacklist [
    "terminal"
    "qutebrowser"
    "google chrome"
    "arc"
    "Brave Browser"
    "Obsidian"
]

# === Session Defaults ===
# :: default : yabai -m config active_window_border_color 0xE032CD32
# reset border color in default mode

# === Resize Mode ===

# TODO: 
# figure out why hyper is not working, karabiners and skhd are not talking??? is hyper evening being registered?

# set resize border color
# :: resize @ : yabai -m config active_window_border_color 0xFFFF00FF
# resize < cmd + alt - r; default
# # toggle resize mode
# default < cmd + alt - r; resize
#
# # resize commands
# resize < left : yabai -m window --resize left:-50:0; \
#                 yabai -m window --resize right:-50:0
#
# resize < down : yabai -m window --resize bottom:0:50; \
#                 yabai -m window --resize top:0:50:
#
# resize < up : yabai -m window --resize top:0:-50; \
#        r        yabai -m window --resize bottom:0:-50
#
# resize < right : yabai -m window --resize right:50:0; \
#                 yabai -m window --resize left:50:0

# === window focus ===
cmd - h : yabai -m window --focus west
cmd - j : yabai -m window --focus south
cmd - k : yabai -m window --focus north
cmd - l : yabai -m window --focus east

# === swap windows ===
shift + cmd - j : yabai -m window --swap south
shift + cmd - k : yabai -m window --swap north
shift + cmd - h : yabai -m window --swap west
shift + cmd - l : yabai -m window --swap east

# === Move window to space ===
shift + alt - 1 : yabai -m window --space 1;
shift + alt - 2 : yabai -m window --space 2;
shift + alt - 3 : yabai -m window --space 3;
shift + alt - 4 : yabai -m window --space 4;
shift + alt - 5 : yabai -m window --space 5;
shift + alt - 6 : yabai -m window --space 6;
shift + alt - 7 : yabai -m window --space 7;
shift + alt - 8 : yabai -m window --space 8;
shift + alt - 9 : yabai -m window --space 9;
shift + alt - 0 : yabai -m window --space 10;

# === Layout ===

# modifying current layout
shift + alt - r : yabai -m space --rotate 270
# filp along y-axis
shift + alt - y : yabai -m space --mirror y-axis
# flip along x-axis
shift + alt - x : yabai -m space --mirror y-axis
# toggle window float
shift + alt - f: yabai -m window --toggle float --grid 4:4:1:1:2:2
# Maximize a window (full screen mode)
shift + alt - m : yabai -m window --toggle zoom-fullscreen
# balance out tree of windows (reset to default)
shift + alt - e : yabai -m space --balance

# TODO: disable sip
# cmd - tab : yabai -m space --focus next || yabai -m space --forcus first
# cmd + shift - tab : yabai -m space --focus prev || yabai -m space --forcus last
# send window and follow focus
# shift + cmd - z : yabai -m window --space prev && yabai -m space --focus prev
# shift + cmd - x : yabai -m window --space recent && yabai -m space --focus recent
# shift + cmd - c : yabai -m window --space next && yabai -m space --focus next
# shift + cmd - 1 : yabai -m window --space 1 && yabai -m space --focus 1
# move focus window to space number #
# cmd - 1 : yabai -m space --focus 1;
# cmd - 2 : yabai -m space --focus 2;
# cmd - 3 : yabai -m space --focus 3;
# cmd - 4 : yabai -m space --focus 4;
# cmd - 5 : yabai -m space --focus 5;
# cmd - 6 : yabai -m space --focus 6;
# cmd - 7 : yabai -m space --focus 7;
# cmd - 9 : yabai -m space --focus 9;
# cmd - 8 : yabai -m space --focus 8;
# cmd - 10 : yabai -m space --focus 10;
#
# create space
# hyper - n : yabai -m space --create; \ sketchybar --update
# destroy current space

# hyper - d : yabai -m space --desotry
# === Manage yabai ===
ctrl + alt + cmd - o : yabai --stop-service; # o for stOp
ctrl + alt + cmd - y : yabai --restart-service; # y for yabai so restart yabai


# Enable / Disable gaps in current workspace
# lcmd - i : yabai -m space --toggle padding; \
#           yabai -m space --toggle gap; \ 
#           yabai -m config external_bar off:0:0; \
#           sketchybar --bar hidden=true
# rcmd - i : yabai -m space --toggle padding; \
#           yabai -m space --toggle gap; \
#           yabai -m config external_bar all:30:0; \
#           sketchybar --bar hidden=false;


# TODO move to karabiners
# === App launch commands ===
cmd - return : open -n -a "wezterm.app"
cmd + shift - return : open -n ~

# === System State Management ===
# lock screen
#
# cmd - escape : pmset displaysleepnow
# # logout
# cmd + shift - e : osascript -e 'tell app "System Events" to log out'
#
# # power down 
# cmd + shift - p : osascript -e 'tell app "System Events" to shut down'
#
# # reboot
# cmd + shift - b : osascript -e 'tell app "System Events" to restart'
#
# # sleep
# cmd + shift - s : osascript -e 'tell app "System Events" to sleep'

